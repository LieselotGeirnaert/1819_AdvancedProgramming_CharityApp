cache:
    paths:
        - .target/

variables:
    REPO_LOC: './spring'

stages:
    - build
    - test
    - package
    - deploy

before_script:
    - cd $REPO_LOC
    - chmod +x ./mvnw

maven-compile:
    stage: build
    script:
        - ./mvnw compile

maven-test:
    stage: test
    script:
        - ./mvnw test

maven-package:
    stage: package
    script:
        - ./mvnw package
    artifacts:
        paths:
            - target/*.war
        expire_in: 1 month

# maven-deploy:
#     stage: deploy $REPO_LOC
#     script:
#         - ./mvnw tomcat7:redeploy -Dmaven.repo.local=$MAVEN_USER_HOME